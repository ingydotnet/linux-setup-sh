#!/usr/bin/env bash

sudo apt-get install git
sudo apt-get install vim
sudo apt-get install exuberant-ctags

pushd $HOME
if [ ! -d ... ]; then
    git clone git://github.com/ingydotnet/....git
    ./.../...
    echo 'sourceif /home/ingy/.bashrc../linux' > .bashrc../local
fi
popd

exec bash
